export class ProfessionalController {
    static async addProfessionalException(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }
    
    static async getProfessionalByID(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }

    static async getProfessionalAvailability(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }

    static async getProfessionalAppointments(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }

    static async updateProfessionalAvailability(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }

    static async updateProfessional(req, res) {
        try {
            
        } catch (error) {
            console.error('Error:', error)
            return res.status(500).json({ error: 'Internal server error'})
        }
    }
}